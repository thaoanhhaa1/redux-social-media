import Skeleton from 'react-loading-skeleton';

const StorySkeleton = () => {
    return (
        <Skeleton containerClassName='flex-shrink-0 w-[110px] aspect-[9/16] rounded-lg overflow-hidden' />
    );
};

export default StorySkeleton;
