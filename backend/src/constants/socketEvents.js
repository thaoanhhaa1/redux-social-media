const on = {
    CONNECTION: 'connection',
    ONLINE: 'online',
    DISCONNECT: 'disconnect',
};
const emit = {
    ONLINE: 'online',
    OFFLINE: 'offline',
};

module.exports = {
    on,
    emit,
};
